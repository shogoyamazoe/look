<div class="container">
  <div class="row">
    <div class="col-auto">
      <h3><i class="fa-solid fa-user"> Senter index</i></h3>
    </div>
  </div>
  <div class="text-right">
    <p>
      <%= link_to new_sent_person_path, class: "btn btn-outline-secondary rounded-pill", method: :get do %>
      <i class="fa-solid fa-user"> new</i>
      <% end %>
    </p>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col">
      <div class="d-grid gap-3">
        <table class="table table-hover">
          <% @sent_persons.each_with_index do |sent_person,i| %>
          <thead class="table-secondary">
            <tr>
              <th>Senter No. <%=i+1%></th>
              <th></th>
            </tr>
          </thead>
          <tr>
            <th><i class="fa-solid fa-user"> name</i></th>
            <td><%= link_to sent_person.sent_person,sent_person_path(sent_person)%></td>
          </tr>
          <tr>
            <th><i class="far fa-handshake"> Connection</i></th>
            <td><%= sent_person.involvement %></td>
          </tr>
          <tr>
            <td>
              <%= link_to edit_sent_person_path(sent_person),class: "btn btn-outline-dark, rounded-circle btn-lg", method: :get do %>
              <i class="far fa-edit"></i>
              <% end %>
              <%= link_to sent_person_path(sent_person),class: "btn btn-danger rounded-circle btn-lg", method: :delete, data: { confirm: "本当に削除しますか？" } do %>
              <i class="far fa-trash-alt fa-1x"></i>
              <% end %></td>
            <td></td>
          </tr>
          <% end %>
        </table>
        <p><%= link_to  root_path, class: "btn btn-default rounded-pill" do %>
          <i class="fas fa-arrow-circle-left"></i>
          <span>back</span>
          <% end %>
        </p>
      </div>
    </div>
  </div>
</div>