<div class="container">
<%= render "shared/error_messages" ,obj: @family_person %>
  <% if flash.now[:notice] %>
  <p class="notice"><%= flash.now[:notice] %></p>
  <% end %>
  <i class=" fa-solid fa-users fa-2x"> Family</i>
</div>
<div class="row">
  <div class="col mx-3">
    <table class="thead-dark">
      <%= form_with model: @family_person, url: {controller: 'family_persons', action: 'create' } ,local: true do |f| %>
      <tr>
        <div class="form-group">
          <th style="width: auto%"><i class=" fa-solid fa-users">　family_person</th>
          <th style="width: 5%">
            <div class="badge badge-danger"> required</div>
          </th>
          <td style="width: auto%"><%= f.text_field :family_person, class: "form-control"%></td>
        </div>
      </tr>
    </table>
    <div>
      <p class="mt-5">
        <%= link_to  family_persons_path, class: "btn btn-default rounded-pill" do %>
        <i class="fas fa-arrow-circle-left"></i>
        <span>back</span>
        <% end %>
        <%= f.button :type => "submit", class: "btn btn-primary rounded-pill" do %>
        <i class="far fa-check-circle"> save</i>
        <% end %>
        <%= link_to root_path, class: "btn btn-secondary rounded-pill" do %>
        <i class="fas fa-home"> home</i>
        <% end %>
      </p>
    </div>
    <% end %>
  </div>
</div>