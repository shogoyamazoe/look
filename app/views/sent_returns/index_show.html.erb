<div class="container">
  <div class="row">
    <div class="col-auto">
      <h3><i class="fas fa-gifts"></i> Gift Return Index</h3>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col">
      <div class="d-grid gap-3">
        <table class="table table-hover">
          <% @sent_returns.each_with_index do |sent_return,i| %>
          <thead class="table-info">
            <tr>
              <th>Return No. <%=i +1 %></th>
              <th></th>
            </tr>
          </thead>
          <tr>
            <th><i class="fas fa-gifts"></i> Gift</th>
            <td><%= link_to sent_return.product, sent_return_path(sent_return)%></td>
          </tr>
          <tr>
            <th><i class="far fa-money-bill-alt"></i> Amount</th>
            <td><%= sent_return.price %></td>
          </tr>
          <tr>
            <th><i class="far fa-comment-dots"></i> Reason</th>
            <td><%= sent_return.introduction %></td>
          </tr>
          <tr>
            <th><i class="far fa-calendar-alt"></i> Date</th>
            <td><%= sent_return.return_day %></td>
          </tr>
          <tr>
            <td>
              <%= link_to edit_sent_return_path(sent_return),class: "btn btn-outline-dark, rounded-circle btn-lg", method: :get do %>
              <i class="far fa-edit"></i>
              <% end %>
              <%= link_to sent_return_path(sent_return),class: "btn btn-danger rounded-circle btn-lg", method: :delete, data: { confirm: "本当に削除しますか？" } do %>
              <i class="far fa-trash-alt fa-1x"></i>
              <% end %>
            </td>
            <td></td>
          </tr>
          <% end %>
        </table>
        <p><%= link_to  sent_items_path, class: "btn btn-default rounded-pill" do %>
          <i class="fas fa-arrow-circle-left"></i>
            Back
          <% end %>
        </p>
      </div>
    </div>
  </div>
</div>