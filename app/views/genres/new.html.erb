<div class="container">
  <%= render "shared/error_messages" ,obj: @genre %>
  <% if flash.now[:notice] %>
  <p class="notice"><%= flash.now[:notice] %></p>
  <% end %>
  <i class="fas fa-layer-group fa-2x"> new_genre</i>
</div>
<%= render "genres/genre_form" %>